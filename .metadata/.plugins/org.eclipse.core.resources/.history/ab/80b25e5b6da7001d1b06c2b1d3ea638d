package setInterface;

import java.util.TreeSet;

public class TreeSetDemo1 {

	public static void main(String[] args) {
		
		TreeSet t = new TreeSet();
		
		t.add(new StringBuffer("A"));
		
		t.add(new StringBuffer("Z"));
		t.add(new StringBuffer("L"));
		t.add(new StringBuffer("B"));
		
		System.out.println(t);//
		
		

	}

}

/*Note::
 * 1. if we are depending on default natural soring order the objects should be homogeneous and comparable
 * otherwise we will get runtime exception saying class cast exception
 * 
 * 2.An object is said to be comparable if and only if the corresponding class implements java.lamng.comparable 
 * interface
 * 
 * 3.String class and all warrper classes already implements comparable interface,but StringBuffer dose't
 * 
 * implement comparable interface.
 * 
 *  Henace in the above program we got class cast exception.
 * 
 * 
 * 
 * 
 * 
 * */
